services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: risetabdimas-app
    restart: unless-stopped
    env_file:
      - .env.docker
    ports:
      - "9090:80"
    networks:
      - risetabdimas

networks:
  risetabdimas:
    driver: bridge
