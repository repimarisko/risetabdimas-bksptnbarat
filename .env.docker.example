# Copy this file to .env.docker and fill in the secrets (APP_KEY, DB_PASSWORD, etc).
APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:9090
TRUSTED_PROXIES=*
#APP_FORCE_HTTPS=false

# Optional (production, behind TLS proxy):
#APP_URL=https://risetabdimas-bksptnbarat.unand.ac.id
#APP_FORCE_HTTPS=true
#ASSET_URL=https://risetabdimas-bksptnbarat.unand.ac.id

LOG_CHANNEL=stack

DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=db_risetabdimas_bksptnbarat
DB_USERNAME=docker
DB_PASSWORD=us3R@dev.2025

BROADCAST_DRIVER=log
CACHE_DRIVER=file
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120
