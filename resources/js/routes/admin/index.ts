import ptPenelitian from './pt-penelitian'
import ptSkema from './pt-skema'

const admin = {
    ptPenelitian: Object.assign(ptPenelitian, ptPenelitian),
    ptSkema: Object.assign(ptSkema, ptSkema),
}

export default admin